<!-- app/views/shared/_navbar.html.erb -->

<nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-end">
  <!-- Navbar content -->
  <!-- <%= link_to 'Home', root_path, class: 'navbar-brand' %> -->
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
    <ul class="navbar-nav">                    
        <% if user_signed_in? %>
        <div class="text-muted">
            <b>Welcome! <%= current_user.email %></b> 
            <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-outline-dark mr-2" %>
        </div>
        <% else %>
        <li class="nav-item">
            <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-outline-dark mr-2" %>
        </li>
      <li class="nav-item">
        <%= link_to 'Sign in', new_user_session_path, class: "btn btn-outline-dark mr-2" %>
      </li>
        <% end %> 

    </ul>
  </div>
</nav>
