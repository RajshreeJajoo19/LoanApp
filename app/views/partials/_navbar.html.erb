<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <!-- Navbar content -->
  <%= link_to loans_path, class: 'no-underline' do %>
    <%= image_tag('logo.png', alt: 'Logo', class: 'navbar-brand logo') %>
       <!-- <b> LOAN APP </b> -->
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item text-muted">
          <b>Welcome! <%= current_user.email %></b>
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-outline-light mr-2", id: "sign_up_btn" %>
          
          <% if current_user.user_role == 2 %>
            <%= link_to 'Update Users', users_path, class: "btn btn-light mr-2", id: "sign_up_btn" %>
          <% else %>
            <%= link_to 'Add Applications', new_loan_path, class: "btn btn-light mr-2", id: "sign_up_btn" %>
          <% end %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-success mr-2", id: "sign_up_btn" %>
          <%= link_to 'Log In', user_session_path, class: "btn btn-success mr-2", id: "sign_up_btn" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
