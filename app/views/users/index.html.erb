<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users Listing Page</title>

    <!-- Add your CSS styles or include a CSS file here -->
</head>

<body>
    <div class="container">
        <h5 class="text-center">Users</h5>
        <br>
        <% if @users.present? %>
        <table class="table" style="width: 100%;">
                <!-- Table header code (unchanged) -->
                <thead style="width: 100%;">
                    <tr>
                        <th>Email</th>
                        <th colspan="3">Action</th>
                    </tr>
                </thead>
                <!-- Table body code -->
                <tbody style="width: 100%;">
                    <% @users.each do |user| %>
                        <tr>
                            <!-- Row data (unchanged) -->
                            <td style="width: 100%;"><%= user.email %></td>
                            <!-- Action buttons (unchanged) -->
                            <td>
                                <%= button_to 'Delete', user_path(user), method: :delete, class: 'btn btn-danger btn-action', onclick: "confirmDelete(event);" %>
                            </td>
                        </tr>
                    <% end %>
                </tbody>
        </table>
        <% else %>
            <div class="no-records">
                <h5>Oops! No records found.</h5>
                <p class="appealing-message">Looks like there are no users to display at the moment. Don't worry, you can always add a new one!</p>
            </div>
        <% end %>
    </div>

    <script>
        function confirmDelete(event) {
            if (!confirm('Are you sure you want to delete?')) {
                event.preventDefault();
            }
        }
    </script>
</body>

</html>
