<% if @loan.errors.any? %>
  // Handle errors, update DOM, display error messages, etc.
  alert("Error: <%= j @loan.errors.full_messages.join(', ') %>");
<% else %>
  // Handle success, redirect, update DOM, etc.
  alert("Model successfully created!");
<% end %>